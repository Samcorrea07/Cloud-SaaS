apiVersion: v1
kind: Service
metadata:
  name: cloudtask-api
  labels:
    app: cloudtask-api
spec:
  type: NodePort
  selector:
    app: cloudtask-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
